#===============================================================================
# Praktiki AI Matching - Environment Configuration
# Copy this file to ai_matching/.env and update values
#===============================================================================

#-------------------------------------------------------------------------------
# API Configuration
#-------------------------------------------------------------------------------
# Backend API URL for fetching data
BACKEND_API_URL=http://YOUR_BACKEND_PRIVATE_IP:8000

# AI Matching service port
API_PORT=8001

#-------------------------------------------------------------------------------
# ML Model Configuration
#-------------------------------------------------------------------------------
# Directory for caching ML models
MODEL_CACHE_DIR=/home/ubuntu/ai_matching/model_cache

# Sentence transformer model to use
# Default: all-MiniLM-L6-v2 (lightweight, good performance)
# Alternatives: all-mpnet-base-v2 (better accuracy, more memory)
TRANSFORMER_MODEL=all-MiniLM-L6-v2

#-------------------------------------------------------------------------------
# Application Settings
#-------------------------------------------------------------------------------
# Debug mode (set to false in production)
DEBUG=false

# Environment (development, staging, production)
ENVIRONMENT=production

# Number of workers (adjust based on available memory)
WORKERS=2

#-------------------------------------------------------------------------------
# Performance Tuning
#-------------------------------------------------------------------------------
# Maximum candidates to rerank (higher = more accurate, slower)
MAX_RERANK_CANDIDATES=10

# Matching score threshold (0.0 - 1.0)
MATCH_THRESHOLD=0.5
